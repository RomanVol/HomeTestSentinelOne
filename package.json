{
  "name": "hometestsentinelone",
  "version": "1.0.0",
  "private": true,
  "description": "Prompt Security browser extension home test automation project.",
  "scripts": {
    "test": "playwright test --project=chrome-extension",
    "test:headed": "playwright test --project=chrome-extension --headed",
    "test:all-browsers": "playwright test",
    "test:chrome": "playwright test --project=chrome-extension",
    "test:edge": "playwright test --project=edge-extension",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}

